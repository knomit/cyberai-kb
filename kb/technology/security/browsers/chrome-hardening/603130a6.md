---
type: observation
domain: [cybersecurity, browsers, detection-engineering]
confidence: 0.85
sources: 1
entities: [Google Chrome, Chrome 136, App-Bound Encryption, Sysmon, SpecterOps]
refs: ['https://developer.chrome.com/blog/remote-debugging-port', 'https://learn.microsoft.com/en-us/windows/security/operating-system-security/sysmon/sysmon-events']
---
# Chrome 136 restricts remote debugging switches on the default data directory to curb cookie theft

Starting with Chrome 136, Google changed remote debugging behavior after observing increased attacker use of Chrome Remote Debugging to extract cookies following the introduction of App-Bound Encryption (ABE). Chrome now ignores the --remote-debugging-port and --remote-debugging-pipe switches when they target the default Chrome data directory unless accompanied by a non-standard --user-data-dir; the non-standard directory uses a different encryption key, which Google says protects Chrome's data from attackers. Google noted cookie theft via the debugging interface had been discussed since 2018. Defenders can detect the newer in-process CDP-enabling technique by looking for process injection targeting chrome.exe and msedge.exe using Sysmon Event IDs 8 and 10.
