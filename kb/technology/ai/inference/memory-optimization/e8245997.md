---
type: concept
domain: [ai, systems, inference, gpu-inference]
confidence: 0.85
sources: 2
entities: [PagedAttention, KV cache, vLLM]
refs: ['https://thegustafson.com/blog/paged-attention', 'kb://d88770a51516/kb/technology/ai/inference/serving/kv-cache/b4f4fbdf.md']
---
# PagedAttention applies virtual-memory paging to the LLM KV cache

The KV cache is a per-request store of attention keys and values that lets a model avoid recomputing them at each decoding step. It grows linearly with sequence length and at long contexts consumes more GPU memory than the model weights themselves, making it the scarce resource in inference serving. Naive contiguous allocation wastes substantial memory through fragmentation and over-reservation. PagedAttention addresses this by implementing virtual-memory-style paging over the KV cache in a form that attention kernels can still operate on.
